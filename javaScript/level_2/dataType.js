/*

el = nos referirnos a element. ejemplo. sum-el los elementos de la suma,
en este proyecto seria:

let firsCard = 2;
let secondCard = 8;

# en value te refieres a id
. en value te refieres a class

si inicias tu valor con ! hace lo contrario. ejemplo:
!isAlive = false;
isAlive = true;

Empty variables are use to store data. example:
let cards = []; // empty array
let player = {}; // empty object
let message = ""; // empty string

Simbolos y sus Nombres

    && = y / and
    || = o / or
    ! = negacion / not
    == = igual a / equal to
    != = no igual a / not equal to
    !== = no estrictamente igual a / strictly
    === = estrictamente igual a / strictly equal to

If we use === we are checking if the value are true or false en we 
use an if statement. example:
if (player.name.length === 0) {
  player.name = prompt("What's your name?");
}

Luego se le asigna un valor segun lo necesitemos.


*/
